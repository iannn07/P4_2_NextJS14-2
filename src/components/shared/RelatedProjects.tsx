import { UserProps } from '@/utils/common.types';

type Props = {
  user: UserProps;
  projectId: string;
};

const RelatedProjects = ({ user, projectId }: Props) => {
  return <div>Hi Sayangkuuuu cintakuuuu</div>;
};

export default RelatedProjects;
